{
  "name": "mailshieldai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd apps/web && pnpm dev",
    "dev:backend": "cd apps/api && uvicorn main:app --reload --port 8000",
    "install:all": "cd apps/web && pnpm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
